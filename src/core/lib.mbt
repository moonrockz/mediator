///|
/// Moonrockz Mediator core: Result type for handler outcomes, PipelineContext and
/// Before/After/Finally middleware (before, after, finally, middleware), and Mediator
/// to send requests through the pipeline. Request/Response enums and dispatch are
/// generated by the `mediator gen` CLI; see the package README for setup.
