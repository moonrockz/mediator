///|
/// Moonrockz Mediator: in-process mediator with codegen-based handler dispatch.
/// Re-exports core (Result, pipeline, Mediator). Request/Response enums and dispatch
/// are generated by the mediator CLI (mediator gen).
